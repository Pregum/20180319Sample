<UserControl x:Class="_20180319Sample.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:_20180319Sample"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="500">
    <Grid>
        <Calendar x:Name="take" SelectedDatesChanged="take_SelectedDatesChanged">
            <Calendar.Background>White</Calendar.Background>
            <Calendar.CalendarDayButtonStyle>
                <Style TargetType="CalendarDayButton">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding}" FontSize="12"/>
                                    <WrapPanel Width="50" Height="60" Margin="2">
                                        <!--<Canvas MouseLeftButtonDown="Canvas_MouseLeftButtonDown">-->
                                            <!--<Image x:Name="Image_Icon" Width="20" Height="20"
                                                   Source="Resources/meat.png" Stretch="Uniform" />-->
                                        <!--</Canvas>-->
                                    </WrapPanel>
                                </StackPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Calendar.CalendarDayButtonStyle>
        </Calendar>
    </Grid>
</UserControl>